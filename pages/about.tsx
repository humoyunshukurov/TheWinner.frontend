import Layout from '../components/Layout';

// Deliberately blank for now - a plain white panel to build into later,
// rather than the sidebar's "About me" link taking students to an
// external portfolio site.
export default function AboutPage() {
  return (
    <Layout>
      <div className="blank-panel" />
    </Layout>
  );
}
